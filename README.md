# stepik-auto-tests-course-final
Final module

#### Проект написан с использованием `python3.7`, встречаются *f-стриги*.

Можно запустить с помощью 
`pytest -v --tb=line --language=en -m need_review`
